{
  "name": "not-your-gig",
  "version": "0.1.0",
  "private": true,
  "description": "Not Your Gig — Proof > Resume. Monorepo root; raw-SQL migrations live in /sql/migrations.",
  "scripts": {
    "db:apply-notice": "node scripts/db-apply-notice.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev"
  }
}

